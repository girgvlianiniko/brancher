# Optional. Without it GitHub repos are public-only and limited to 60 requests an hour,
# and branch lists come without last-commit details.
GITHUB_TOKEN=

# Folder scanned for git repos on first start. Defaults to the folder brancher sits in.
# BRANCHER_SCAN_DIR=

# BRANCHER_PORT=4317
